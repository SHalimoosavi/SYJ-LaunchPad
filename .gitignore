# --- Environments / secrets ---
.env
.env.*
!.env.example

# --- Python / backend ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node / frontend ---
node_modules/
.next/
out/
dist/
*.tsbuildinfo

# --- Contracts ---
# --- Contracts (Hardhat) ---
contracts/artifacts/
contracts/cache/
contracts/typechain-types/
contracts/coverage/
contracts/coverage.json

# --- Contracts (Foundry) ---
contracts/out/
contracts/cache-forge/
# forge-std (and any future forge dependency) is fetched via `forge
# install`, not vendored — same convention as node_modules/.venv being
# reinstalled by CI rather than committed.
contracts/lib/

# --- OS / editor ---
.DS_Store
*.log
.vscode/
.idea/
